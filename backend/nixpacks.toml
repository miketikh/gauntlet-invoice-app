# Railway Nixpacks Configuration for Java 21
[phases.setup]
nixPkgs = ["openjdk21", "maven"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[phases.start]
cmd = "java -Xmx512m -Xms256m -jar target/*.jar"

[start]
cmd = "java -Xmx512m -Xms256m -jar target/*.jar"
